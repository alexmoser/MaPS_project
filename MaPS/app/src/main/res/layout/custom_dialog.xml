<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSearchResultTitle"
        android:text="@string/title_quantity_dialog"
        android:id="@+id/dialog_tv_title"
        android:layout_alignParentTop="true"/>

    <NumberPicker
        android:id="@+id/dialog_np"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:focusableInTouchMode="false"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/dialog_tv_title"
        android:layout_marginTop="15dp"/>

    <Button
        android:id="@+id/dialog_bt_ok"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/dialog_np"
        android:layout_marginTop="15dp"
        android:layout_alignParentRight="true"
        android:text="@string/dialog_ok"
        android:textColor="@color/accent_maps"
        android:background="@android:color/transparent"
        style="?android:borderlessButtonStyle"
        android:textAlignment="textEnd"
        android:paddingRight="20dp"
        android:paddingLeft="1dp"/>

    <Button
        android:id="@+id/dialog_bt_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/dialog_bt_ok"
        android:layout_alignBottom="@+id/dialog_bt_ok"
        android:text="@string/dialog_cancel"
        android:textColor="@color/accent_maps"
        android:background="@android:color/transparent"
        style="?android:borderlessButtonStyle"
        android:layout_toLeftOf="@id/dialog_bt_ok"
        android:textAlignment="textEnd"/>

</RelativeLayout>